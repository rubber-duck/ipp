schema_version = "1.0.0"
id = "ipp_blender"
version = "0.1.0"
name = "IPP Scene Sync"
tagline = "Preview authored scenes in the IPP browser runtime"
maintainer = "IPP contributors"
type = "add-on"
blender_version_min = "5.2.0"
blender_version_max = "5.3.0"
license = ["SPDX:GPL-3.0-or-later"]

[permissions]
network = "Serve scene assets and updates on HTTPS localhost"
files = "Retain local TLS credentials and immutable scene exports"
